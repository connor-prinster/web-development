class Constants {

    constructor() {
        const basePath = './design/'
        const imageBasePath = basePath + 'images/'

        this.paths = {
            landerPath: imageBasePath + 'lander.png'
        }
        this.math = {
            landerSizeRatio: .1
        }
    }
}